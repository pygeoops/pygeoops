name: pygeoops-dev
channels:
  - conda-forge
dependencies:
  - python =3.10
  - pip
  # required
  - geopandas
  - shapely >1
  - topojson
  # testing
  - matplotlib
  - pytest
  - pytest-cov
  # linting
  - black
  - pre-commit
  - ruff
  # docs
  # Restrict sphinx version to < 6 to avoid logo error
  - sphinx <6
  - pydata-sphinx-theme
  - pip:
    - sphinx-automodapi ==0.13
    - simplification
