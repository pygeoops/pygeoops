name: test
channels:
  - conda-forge
dependencies:
  - python
  - pip
  # required
  - geopandas-base >=0.12.1
  - shapely >1
  - topojson
  # testing
  - matplotlib
  - pytest
  - pytest-cov
  - pip:
    - simplification  # optional dependency