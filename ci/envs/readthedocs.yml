name: pygeoops-doc
channels:
  - conda-forge
dependencies:
  - python =3.12
  - pip  # Is apparently needed even though there are no pip packages specified
  # required
  - geopandas-base >=0.12.1
  - numpy
  - pyproj
  - shapely >1
  - topojson
  # optional
  - simplification
  # docs
  - matplotlib-base
  - pydata-sphinx-theme
  - sphinx
  - sphinx-automodapi
  - sphinx-copybutton